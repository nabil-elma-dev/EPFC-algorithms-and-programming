1) What do the programs do (see "For - Instructions in French.pdf)"? (Header lines are omitted). Indent them first.
   Can you clearly explain what the last program (h) computes?

2) Write a program that displays the multiplication table of a number entered by the user.
   Example: if the user enters 5, the output is:
    1 × 5 = 5
    2 × 5 = 10
    ...
    10 × 5 = 50

3) Rewrite a program that computes the average of n numbers entered by the user.
   The value of n is given as well by the user.

4) Write a program that determines the minimum of a series of n integers entered by the user.

   Example:
    8
    5 9 -1 0 12 -7 -4 3

   Output: -7

5) Write a program that counts the number of positive, negative, and zero values among n numbers entered by the user.
   Example:
    8
    5 9 -1 0 12 -7 -4 3

   Output:
    - positives: 4
    - negatives: 3
    - zeros: 1

6) Write a program that computes the average of the positive numbers among n inputs.
   Example:
    8
    5 9 -1 0 12 -7 -4 3

   Output: 7.25

7) Write a program that computes the factorial of an integer entered by the user. The program must verify that the number is positive.
   Otherwise, display an error message

   Reminder: n! = n × (n - 1) × ... × 2 × 1

8) Write a program that computes the power of two integers entered by the user (a^b).
   That is: a * a * a * ... * a   (b - 1 multiplications)

   Take into account:
    - 0⁰ is undefined
    - If b is negative: a^b = 1 / a^(-b)